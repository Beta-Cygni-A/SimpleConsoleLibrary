#include <iostream>

int main ()
{
   int temp_level_length {0};
   read level_info.txt file for level length and store in var above
   
   have std:vector of type char, size controlled by var above
   
   put it all in a function that takes y row and x row wanted and goes through art file line by line till get to right y row temp and then stores that in vector and then goes throigh that to find right x row temp and then retur s that to be used as a nee value for a char on gridsystem.
   
   return 0;
}